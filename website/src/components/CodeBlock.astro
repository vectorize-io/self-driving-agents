---
interface Props {
  code: string;
  lang?: string;
}
const { code, lang = 'bash' } = Astro.props;
---

<pre class="overflow-x-auto rounded-lg bg-ink-900 px-4 py-3 text-sm text-ink-100">
<code data-lang={lang}>{code}</code>
</pre>
